# Archived V8 command record

V8 command documentation is intentionally not retained in the current skill.
V8, V8.1, V8.2, and V9 artifacts may be read only as frozen evidence through
the V10 migration and comparison workflow. They are not runnable methodologies.

Use `python scripts/v10_cli.py TOOL ...` for every operation. Pinned revisions
are provenance evidence only; do not execute them as an alternative workflow or
mix historical outputs into a V10 study without the typed V10 migration.
