{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "evaluation-state.schema.json",
  "title": "Subject index evaluation state",
  "type": "object",
  "required": ["schema_version", "evaluation_id", "source", "configuration", "stages", "artifacts", "blockers"],
  "properties": {
    "schema_version": {"const": "subject-index-evaluation-state-v6"},
    "evaluation_id": {"type": "string", "minLength": 1},
    "created_at": {"type": "string"},
    "updated_at": {"type": "string"},
    "source": {
      "type": "object",
      "required": ["title", "filename", "sha256", "document_page_span"],
      "properties": {
        "title": {"type": "string"},
        "edition": {"type": "string", "minLength": 1},
        "edition_identity": {
          "type": "object",
          "required": ["label", "status"],
          "properties": {
            "label": {"type": "string", "minLength": 1},
            "status": {"enum": ["verified", "not_independently_verified", "conflicting_evidence"]},
            "rationale": {"type": "string"}
          },
          "additionalProperties": true
        },
        "filename": {"type": "string"},
        "sha256": {"type": ["string", "null"], "pattern": "^[a-f0-9]{64}$"},
        "document_page_span": {
          "type": "array",
          "prefixItems": [{"type": "integer"}, {"type": "integer"}],
          "minItems": 2,
          "maxItems": 2
        }
      },
      "additionalProperties": true
    },
    "candidate": {
      "type": ["object", "null"],
      "required": ["candidate_id", "sha256", "schema_version", "normalized_path", "item_inventory_path", "benchmark_path", "benchmark_sha256"],
      "properties": {
        "candidate_id": {"type": "string", "minLength": 1},
        "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
        "schema_version": {"const": "candidate-index-v2"},
        "normalized_path": {"$ref": "#/$defs/relativePath"},
        "item_inventory_path": {"$ref": "#/$defs/relativePath"},
        "benchmark_path": {"$ref": "#/$defs/relativePath"},
        "benchmark_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
      },
      "additionalProperties": true
    },
    "configuration": {
      "type": "object",
      "required": ["audit_mode", "index_type", "intended_readership", "readership_provenance", "output_format", "storage_mode", "policy_profile", "rubric_version", "scoring_identity"],
      "properties": {
        "audit_mode": {"enum": ["full", "pilot"]},
        "index_type": {"const": "subject_index"},
        "intended_readership": {"type": "string"},
        "readership_provenance": {
          "type": "object",
          "required": ["basis", "confidence", "rationale"],
          "properties": {
            "basis": {"enum": ["inferred", "user_supplied"]},
            "confidence": {"enum": ["high", "medium", "low"]},
            "rationale": {"type": "string", "minLength": 1}
          },
          "additionalProperties": true
        },
        "output_format": {"const": "json"},
        "storage_mode": {"enum": ["local", "library", "hybrid"]},
        "policy_profile": {"const": "subject-index-standard-policy-v8"},
        "rubric_version": {"const": "subject-index-rubric-v8"},
        "scoring_identity": {
          "type": "object",
          "required": ["rubric_version", "dimension_calculation_profile"],
          "properties": {
            "rubric_version": {"const": "subject-index-rubric-v8"},
            "dimension_calculation_profile": {"const": "subject-index-dimension-calculation-v4"}
          },
          "additionalProperties": false
        },
        "score_profile_history": false
      },
      "additionalProperties": true
    },
    "stages": {
      "type": "object",
      "required": ["initialize", "page_mapping", "chunk_definition", "define_policy", "source_chunk_preparation", "source_subject_discovery", "benchmark_synthesis", "benchmark_review", "benchmark_freeze", "candidate_normalization", "locator_chunk_preparation", "locator_audit", "missing_access_audit", "structure_audit", "scoring", "web_report"],
      "properties": {
        "initialize": {"$ref": "#/$defs/stage"},
        "page_mapping": {"$ref": "#/$defs/stage"},
        "chunk_definition": {"$ref": "#/$defs/stage"},
        "define_policy": {"$ref": "#/$defs/stage"},
        "source_chunk_preparation": {"$ref": "#/$defs/stage"},
        "source_subject_discovery": {"$ref": "#/$defs/stage"},
        "benchmark_synthesis": {"$ref": "#/$defs/stage"},
        "benchmark_review": {"$ref": "#/$defs/stage"},
        "benchmark_freeze": {"$ref": "#/$defs/stage"},
        "candidate_normalization": {"$ref": "#/$defs/stage"},
        "locator_chunk_preparation": {"$ref": "#/$defs/stage"},
        "locator_audit": {"$ref": "#/$defs/stage"},
        "missing_access_audit": {"$ref": "#/$defs/stage"},
        "structure_audit": {"$ref": "#/$defs/stage"},
        "scoring": {"$ref": "#/$defs/stage"},
        "web_report": {"$ref": "#/$defs/stage"}
      },
      "additionalProperties": false
    },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["artifact_id", "stage", "artifact_type", "path", "sha256", "media_type", "visibility", "retention", "frozen", "recorded_at"],
        "properties": {
          "stage": {"enum": ["initialize", "page_mapping", "chunk_definition", "define_policy", "source_chunk_preparation", "source_subject_discovery", "benchmark_synthesis", "benchmark_review", "benchmark_freeze", "candidate_normalization", "locator_chunk_preparation", "locator_audit", "missing_access_audit", "structure_audit", "scoring", "web_report"]},
          "artifact_type": {"type": "string"},
          "path": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+"
          },
          "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "media_type": {"type": "string", "minLength": 1},
          "schema_version": {"type": "string", "minLength": 1},
          "artifact_id": {"type": "string", "pattern": "^ART-[A-F0-9]{12}$"},
          "visibility": {"enum": ["public", "private", "restricted"]},
          "retention": {"enum": ["required", "cache"]},
          "frozen": {"type": "boolean"},
          "recorded_at": {"type": "string"},
          "input_sha256": {"type": "array", "items": {"type": "string", "pattern": "^[a-f0-9]{64}$"}}
        },
        "additionalProperties": true
      }
    },
    "blockers": {"type": "array", "items": {"type": "object"}}
  },
  "$defs": {
    "stage": {
      "type": "object",
      "required": ["status", "updated_at", "notes"],
      "properties": {
        "status": {"enum": ["not_started", "in_progress", "completed", "blocked"]},
        "updated_at": {"type": ["string", "null"]},
        "notes": {"type": "array", "items": {"type": "string"}}
      },
      "additionalProperties": false
    },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+"
    }
  },
  "additionalProperties": true
}
